<RibbonWindow x:Class="ImageToolApp.MainWindow"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              mc:Ignorable="d"
              Title="ImageTool" Height="500" Width="600" Loaded="RibbonWindow_Loaded">

    <RibbonWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/ResourceDictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </RibbonWindow.Resources>

    <DockPanel LastChildFill="True">
        <Ribbon DockPanel.Dock="Top" SelectedIndex="{Binding SelectedRibbonTabIndex, Mode=OneWayToSource}">
            <Ribbon.HelpPaneContent>
                <RibbonButton SmallImageSource="{StaticResource HelpImage}" />
            </Ribbon.HelpPaneContent>

            <Ribbon.ApplicationMenu>
                <RibbonApplicationMenu x:Name="RibbonAppMenu" SmallImageSource="{StaticResource MenuImage}">
                    <RibbonApplicationMenuItem Header="Einstellungen" ImageSource="{StaticResource PreferencesImage}"
                                               Command="{Binding PreferencesCommand}" />
                    <RibbonApplicationMenuItem Header="Beenden" ImageSource="{StaticResource CloseImage}"
                                               Command="{Binding CloseAppCommand}" />
                </RibbonApplicationMenu>
            </Ribbon.ApplicationMenu>

            <RibbonTab Header="Encrypt">
                <RibbonGroup Header="Bild">
                    <RibbonButton Label="Öffnen" Command="{Binding OpenImageCommand}" SmallImageSource="{StaticResource OpenImgImage}"/>
                    <RibbonButton Label="Speichern" Command="{Binding SaveImageCommand}" SmallImageSource="{StaticResource SaveImage}"/>
                </RibbonGroup>
                <RibbonGroup Header="Text">
                    <RibbonButton Label="Öffnen" Command="{Binding OpenTxtCommand}" SmallImageSource="{StaticResource OpenTxtImage}"/>
                    <!--<RibbonButton Label="Speichern" Command="{Binding SaveTxtCommand}" SmallImageSource="{StaticResource SaveImage}"/>-->
                </RibbonGroup>
            </RibbonTab>

            <RibbonTab Header="Decrypt">
                <RibbonGroup Header="Bild">
                    <RibbonButton Label="Öffnen" Command="{Binding OpenImageCommand}" SmallImageSource="{StaticResource OpenImgImage}"/>
                    <!--<RibbonButton Label="Speichern" Command="{Binding SaveImageCommand}" SmallImageSource="{StaticResource SaveImage}"/>-->
                </RibbonGroup>
                <RibbonGroup Header="Text">
                    <!--<RibbonButton Label="Öffnen" Command="{Binding OpenTxtCommand}" SmallImageSource="{StaticResource OpenTxtImage}"/>-->
                    <RibbonButton Label="Speichern" Command="{Binding SaveTxtCommand}" SmallImageSource="{StaticResource SaveImage}"/>
                </RibbonGroup>
            </RibbonTab>
        </Ribbon>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" MaxWidth="200" />
            </Grid.ColumnDefinitions>
            <ContentControl Grid.Column="0" Content="{Binding CurrentElement}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            <Expander Grid.Column="1" ExpandDirection="Left" Width="Auto" Height="Auto" IsExpanded="False"
                      VerticalAlignment="Top" HorizontalAlignment="Right">
                <Image Source="{Binding GlobalViewModel.ImagePath}" Stretch="Uniform" />
            </Expander>
        </Grid>
    </DockPanel>
</RibbonWindow>